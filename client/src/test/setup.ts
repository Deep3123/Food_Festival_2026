/**
 * Vitest setup for the ByteBites client test suite.
 *
 * Registers @testing-library/jest-dom matchers (e.g. toBeInTheDocument,
 * toBeDisabled) so component tests can make expressive DOM assertions.
 */

import "@testing-library/jest-dom/vitest";
