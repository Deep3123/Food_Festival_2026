{
  "name": "bytebites",
  "version": "0.1.0",
  "private": true,
  "description": "Invest-a-Bite - Invest in Taste. Earn in Happiness.",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "tsc --build",
    "build:vercel": "vite build client",
    "typecheck": "tsc --build --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:pbt": "vitest run --dir . -t \"Property\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "fast-check": "^3.22.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
